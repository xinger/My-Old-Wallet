Vue.component('DrivesList', {
    props: ['Drives'],
    template:  __modules_config['module.DrivesModel'].tpl.DriveList
    // methods: {
    //     hi: function(item) {
    //         this.$root.$emit('drive_click', item);
    //     }
    // }
});
