Air.define('module.main_window', 'lib.DOM', function($) {
    var self = this,
        vue = require('vue');

    self.init = function() {
    };

    self.refresh = function() {

    };

    self.destroy = function() {

    };

});
